-- Create Database (SQLite automatically creates when opened)
-- Command used: sqlite3 internship.db

-- Create Table
CREATE TABLE interns (
    id INTEGER PRIMARY KEY,
    name TEXT,
    track TEXT,
    stipend INTEGER
);

-- Insert Data
INSERT INTO interns (id, name, track, stipend)
VALUES (1, 'Dish', 'Data Science', 15000);

INSERT INTO interns (id, name, track, stipend)
VALUES (2, 'Rahul', 'Web Dev', 12000);

INSERT INTO interns (id, name, track, stipend)
VALUES (3, 'Sush', 'Data Science', 18000);

INSERT INTO interns (id, name, track, stipend)
VALUES (4, 'Pavana', 'Web Dev', 10000);

INSERT INTO interns (id, name, track, stipend)
VALUES (5, 'Heera', 'AI/ML', 20000);

-- Select Queries
SELECT * FROM interns;

SELECT name, track FROM interns;-- Create Database (SQLite automatically creates when opened)
-- Command used: sqlite3 internship.db

-- Create Table
CREATE TABLE interns (
    id INTEGER PRIMARY KEY,
    name TEXT,
    track TEXT,
    stipend INTEGER
);

-- Insert Data
INSERT INTO interns (id, name, track, stipend)
VALUES (1, 'Dish', 'Data Science', 15000);

INSERT INTO interns (id, name, track, stipend)
VALUES (2, 'Rahul', 'Web Dev', 12000);

INSERT INTO interns (id, name, track, stipend)
VALUES (3, 'Rohan', 'Data Science', 18000);

INSERT INTO interns (id, name, track, stipend)
VALUES (4, 'Sush', 'Web Dev', 10000);

INSERT INTO interns (id, name, track, stipend)
VALUES (5, 'Rishi', 'AI/ML', 20000);

-- Select Queries
SELECT * FROM interns;

SELECT name, track FROM interns;
